{
  "id": "epg-enricher",
  "name": "EPG Enricher",
  "version": "1.13.8",
  "api_version": "1.0.0",
  "description": "Enriches EPG programme data with TMDB artwork (posters, backdrops), genres and descriptions. Only processes channels that are mapped in your playlists, complementing existing Schedules Direct / Gracenote metadata where data is missing.",
  "repository": "Serph91P/m3u-editor-epg-enricher",
  "entrypoint": "Plugin.php",
  "class": "AppLocalPlugins\\EpgEnricher\\Plugin",
  "capabilities": [
    "epg_processor"
  ],
  "hooks": [
    "epg.cache.generated"
  ],
  "permissions": [
    "db_read",
    "filesystem_read",
    "filesystem_write",
    "network_egress",
    "hook_subscriptions",
    "queue_jobs"
  ],
  "settings": [
    {
      "id": "tmdb_config",
      "type": "section",
      "label": "TMDB Configuration",
      "description": "TMDB API source settings and general behavior.",
      "collapsible": true,
      "collapsed": true,
      "fields": [
        {
          "id": "enrich_from_tmdb",
          "type": "boolean",
          "label": "Enrich programmes with TMDB data",
          "default": true,
          "helper_text": "Fetch posters, backdrops, genres and descriptions from TMDB for programmes that are missing artwork."
        },
        {
          "id": "tmdb_language",
          "type": "text",
          "label": "TMDB search language",
          "default": "",
          "helper_text": "Language for TMDB searches (e.g. 'de-DE', 'en-US', 'fr-FR'). Leave empty to use the global TMDB language from general settings."
        },
        {
          "id": "overwrite_existing",
          "type": "boolean",
          "label": "Overwrite existing metadata",
          "default": false,
          "helper_text": "Replace existing artwork and descriptions even if the programme already has data (e.g. from Schedules Direct)."
        },
        {
          "id": "allow_title_only_lookup",
          "type": "boolean",
          "label": "Allow cautiously validated title-only lookup",
          "default": false,
          "helper_text": "Opt in to bounded TMDB candidate discovery when source year, ID, and episode evidence are absent. Ambiguous and sports/event matches remain rejected."
        }
      ]
    },
    {
      "id": "enrichment_options",
      "type": "section",
      "label": "Enrichment Options",
      "description": "Choose which metadata fields to enrich from TMDB.",
      "collapsible": true,
      "collapsed": true,
      "fields": [
        {
          "id": "enrich_categories",
          "type": "boolean",
          "label": "Enrich missing categories/genres",
          "default": true,
          "helper_text": "Fill in missing genre/category from TMDB when the programme has none."
        },
        {
          "id": "overwrite_categories",
          "type": "select",
          "label": "Category replacement",
          "default": "inherit",
          "options": [
            { "value": "inherit", "label": "Inherit overwrite existing" },
            { "value": "missing_only", "label": "Missing only" },
            { "value": "replace", "label": "Replace" }
          ],
          "helper_text": "Controls replacing categories from a TMDB match. Existing mapper normalization is separate and does not fetch or replace TMDB categories."
        },
        {
          "id": "enrich_descriptions",
          "type": "boolean",
          "label": "Enrich missing descriptions",
          "default": true,
          "helper_text": "Fill in missing programme descriptions from TMDB."
        },
        {
          "id": "overwrite_descriptions",
          "type": "select",
          "label": "Description replacement",
          "default": "inherit",
          "options": [
            { "value": "inherit", "label": "Inherit overwrite existing" },
            { "value": "missing_only", "label": "Missing only" },
            { "value": "replace", "label": "Replace" }
          ],
          "helper_text": "Controls replacing existing descriptions independently of artwork and categories."
        },
        {
          "id": "enrich_episode_details",
          "type": "boolean",
          "label": "Use episode-level details for TV series",
          "default": true,
          "helper_text": "For episodic TV programmes with season/episode data, use TMDB episode overview and screenshots when available."
        },
        {
          "id": "enrich_posters",
          "type": "boolean",
          "label": "Enrich poster/icon artwork",
          "default": true,
          "helper_text": "Fetch poster images from TMDB as typed artwork."
        },
        {
          "id": "overwrite_artwork",
          "type": "select",
          "label": "Artwork replacement",
          "default": "inherit",
          "options": [
            { "value": "inherit", "label": "Inherit overwrite existing" },
            { "value": "missing_only", "label": "Missing only" },
            { "value": "replace", "label": "Replace" }
          ],
          "helper_text": "Controls replacing valid provider artwork independently of descriptions and categories."
        },
        {
          "id": "primary_artwork",
          "type": "select",
          "label": "Primary artwork",
          "default": "legacy",
          "options": [
            { "value": "legacy", "label": "Legacy landscape preference" },
            { "value": "poster", "label": "Poster first" },
            { "value": "backdrop", "label": "Backdrop first" }
          ],
          "helper_text": "Select the untyped primary icon when artwork replacement is permitted. Disabled artwork types are never selected."
        },
        {
          "id": "enrich_backdrops",
          "type": "boolean",
          "label": "Enrich backdrop images",
          "default": true,
          "helper_text": "Fetch backdrop (landscape) images from TMDB and add them to the programme images array."
        }
      ]
    },
    {
      "id": "genre_detection",
      "type": "section",
      "label": "Genre & Detection",
      "description": "Genre mapping and keyword-based category detection.",
      "collapsible": true,
      "collapsed": true,
      "fields": [
        {
          "id": "map_genres_to_epg_categories",
          "type": "boolean",
          "label": "Map genres to Emby compatible EPG categories",
          "default": false,
          "helper_text": "Normalize TMDB genres to the compact category names used by Emby-style guide coloring: Movie, Series, News, Sports, Kids, Documentary, Music, Education."
        },
        {
          "id": "map_genres_to_kodi_guide_genres",
          "type": "boolean",
          "label": "Map genres to Kodi guide color genres",
          "default": false,
          "helper_text": "Use the detailed Kodi genre labels from m3u-editor issue 347, such as Basketball, Public Affairs, Comedy-Drama, Science Fiction, Cooking, and Game Show. When enabled, this takes precedence over the compact Emby compatible category option."
        },
        {
          "id": "keyword_category_detection",
          "type": "boolean",
          "label": "Detect categories from title keywords",
          "default": true,
          "helper_text": "Detect Sports, News, Kids, and Documentary categories directly from programme titles. Active when either the Emby compatible or Kodi guide genre mapper is enabled."
        }
      ]
    },
    {
      "id": "automation",
      "type": "section",
      "label": "Automation",
      "description": "Automatic enrichment triggers and playlist scope.",
      "collapsible": true,
      "collapsed": true,
      "fields": [
        {
          "id": "auto_run_on_cache",
          "type": "boolean",
          "label": "Auto-run after EPG cache generation",
          "default": true,
          "helper_text": "Automatically enrich EPG data whenever the cache is rebuilt."
        },
        {
          "id": "auto_run_playlists",
          "type": "select",
          "label": "Limit auto-run to these playlists",
          "options_provider": "enrichable_playlists",
          "multiple": true,
          "required": false,
          "helper_text": "When a related EPG cache is generated, fully enrich all EPG sources used by these playlists. Leave empty to include all related playlists."
        }
      ]
    }
  ],
  "actions": [
    {
      "id": "enrich_epg",
      "label": "Enrich EPG",
      "icon": "heroicon-o-sparkles",
      "requires_confirmation": true,
      "fields": [
        {
          "id": "playlist_id",
          "type": "select",
          "label": "Playlist",
          "options_provider": "enrichable_playlists",
          "required": true
        }
      ]
    },
    {
      "id": "health_check",
      "label": "Health Check",
      "icon": "heroicon-o-heart",
      "dry_run": true,
      "fields": []
    },
    {
      "id": "clear_state",
      "label": "Clear Enrichment Cache",
      "icon": "heroicon-o-trash",
      "requires_confirmation": true,
      "fields": []
    }
  ],
  "data_ownership": {
    "directories": [
      "plugin-data/epg-enricher"
    ],
    "tables": [],
    "files": [
      "plugin-data/epg-enricher/enrichment-state.json",
      "plugin-data/epg-enricher/tmdb-season-cache.json"
    ],
    "default_cleanup_policy": "preserve"
  }
}
